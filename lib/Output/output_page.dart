import 'package:flutter/material.dart';

class OutputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('カレンダー表示して日付ごとに読書の達成を確認できる画面'),
    );
  }
}

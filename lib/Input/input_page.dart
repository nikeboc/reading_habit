import 'package:flutter/material.dart';

class InputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('一定時間の読書を達成した時にボタンで記録するページ'),
    );
  }
}
